export const revalidate = 60;

import React from "react";
import AgamEbooks from "../(home)/(sections)/AgamEbooks";

const Page = async () => {
  return (
    <div>
      <AgamEbooks />
    </div>
  );
};

export default Page;
